dist/
node_modules/
pnpm-lock.yaml
data/
docs/

# Retired packages kept only as reading material (see archive/README.md). They are
# outside the workspace globs, so nothing builds or tests them; reformatting them
# on a prettier upgrade would only add noise to code nobody is going to change.
archive/

# A committed BUNDLE is an artifact, not source: reformatting it would give it a
# different identity (the hash of its octets IS its name, ADR-0086) while changing
# nothing about what it does -- and `--minify` is mandatory for that identity to be
# stable across machines, so the one-line shape is the point.
packages/utils/test/fixtures/processor-artifact/processor.bundle.js
packages/cli/test/fixtures/processor-bundle/nfts.bundle.js
packages/cli/test/fixtures/processor-bundle/nfts-edited.bundle.js
packages/cli/test/fixtures/processor-bundle/nfts-with-approval.bundle.js
packages/cli/test/fixtures/processor-bundle/not-self-contained.bundle.js
packages/cli/test/fixtures/processor-bundle/throws-on-evaluation.bundle.js

*.md
*.yaml
*.html
*.json
