---
'etherfold': patch
---

Docs: the CLI README points at `examples/node-dev-loop`, a runnable dev loop (anvil, a contract, `etherfold node`, and `as-soon` rebuilding and uploading the processor on every save).
