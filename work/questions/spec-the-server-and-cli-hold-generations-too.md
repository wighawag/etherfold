<!-- dorfl-sidecar: item=spec:the-server-and-cli-hold-generations-too type=spec slug=the-server-and-cli-hold-generations-too allAnswered=false -->

## Q1

**'spec:the-server-and-cli-hold-generations-too' was bounced — how should we proceed?**

> The tasker review→edit loop could not converge on a sound decomposition of 'the-server-and-cli-hold-generations-too' (--tasker-loop-max exhausted with unresolved blockers). The spec is parked for your attention with no guessed tasks; resolve:
> - Where does a NON-CANONICAL generation's state live on ONE SQL database? Verified in-tree: VersionedStateStoreOptions (state-store-sqlite/src/store.ts:63) exposes only bounds/retention/finalityDepth, ddlForEntity names tables from the declared entity name, FIXED_SCHEMA_DDL adds fixed _blocks/_cursor, and buildProcessor (cli/src/folding.ts:95) opens one handle for one store. Choose among per-generation table prefix, database-per-generation (impossible on D1, whose bindings are static, and it breaks the one-handle property run relies on for /status and serving), or a generation column on every versioned row. The choice also decides what the registry port's dropState does here, what the read tier resolves a read through, and whether serve, which holds no processor, can find the canonical generation's tables.
> - Does the INDEXER NAME use the SAME mechanism at a second level of that key, or a handle per name with generations nested inside it? Entity tables carry no discriminator today, so two named indexers with identical sources would write one another's rows, and story 5's guarantee (deleting everything in one leaves the other complete and readable) cannot be expressed. Answering this together with the previous question is what makes the composite key structural rather than conventional.
> - What IS the SQL stream keeper? _emissions is one row per log keyed (indexer, stream, seq) with no cursor row, its writer appendEmissions is called from the ingest route because only the route segment holds the indexer name, while createSegmentedStream is one segment per batch under an ordinal with a cursor record. Does the keeper BECOME that writer (and then what owns the name), or read what the route writes and hold only its own cursor? Where does that cursor row live, given the contract requires it to move in the SAME transaction as the append the route currently owns? And does it ride createSegmentedStream over a SQL port, or implement ExistingStream directly over seq, in which case the shared conformance material has nothing to attach to? ADR-0044's one-writer rule makes this a correctness question.
> - How much of story 9's serverless HOST does this spec owe? platforms/cf-worker/src is three files, hosts no processor, owns no store, and has neither a queue binding nor a cron handler, yet ADR-0008 chose a self-enqueueing queue with a cron watchdog. Decide whether story 9 means the platform-neutral bounded-chunk driver here (checkpoint durable in a row, resumability asserted by driving chunk after chunk through a fresh container, like prune and compactEmissionPairs) with the Worker wiring elsewhere, or both here.
> - Which surface makes a rebuild in progress distinguishable from an empty result (story 4)? The query layer is deferred deliberately and /status is the whole query surface today. Candidates, none pinned: a per-generation dimension inside /status's cursor envelope (ADR-0047 left room), the generation value both feed views already advertise, or a refusal on a read against a generation that has not caught up.
> - Reframe shape: should this become an EXPLORATION spec whose done is CONFIDENCE plus a de-risked sliced BUILD PLAN (pin the state-namespace seam, spike two generations under one handle on the narrowest real case against both libSQL and D1 constraints, pin the keeper seam against the emission table that exists, decide the host boundary and the observable surface, emit the plan), with the capability BUILD as a follow-on spec ordered taskedAfter it, and question 10 possibly carved out entirely?

<!-- q1 fields: id=q1 kind=stuck -->

**Your answer** (write below this line):
