<!-- dorfl-sidecar: item=task:the-chain-facing-container-holds-its-generations-in-slots type=task slug=the-chain-facing-container-holds-its-generations-in-slots allAnswered=false -->

## Q1

**'task:the-chain-facing-container-holds-its-generations-in-slots' was bounced — how should we proceed?**

> continuing the kept work/task-the-chain-facing-container-holds-its-generations-in-slots: rebase onto the latest main conflicted (aborted, never auto-resolved) — run `requeue --reconcile` to non-destructively re-sync the mirror and retry the rebase (keeps the work). Last resort: `requeue --reset` DESTRUCTIVELY discards the branch and starts fresh.

<!-- q1 fields: id=q1 kind=stuck -->

**Your answer** (write below this line):
